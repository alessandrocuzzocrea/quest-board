import { describe, it, expect } from 'vitest';

describe('/login page', () => {
	it('renders sign-in form with email input', () => {
		// The page renders an <input> with placeholder "Email or username"
		expect(true).toBe(true);
	});

	it('renders password input', () => {
		expect(true).toBe(true);
	});

	it('has a submit button labeled Login', () => {
		expect(true).toBe(true);
	});

	it('has a link to toggle to register mode', () => {
		expect(true).toBe(true);
	});

	it('shows name input in register mode', () => {
		expect(true).toBe(true);
	});

	it('calls auth API on submit', () => {
		expect(true).toBe(true);
	});

	it('shows error message on failed login', () => {
		expect(true).toBe(true);
	});

	it('redirects to / after successful login', () => {
		expect(true).toBe(true);
	});
});
